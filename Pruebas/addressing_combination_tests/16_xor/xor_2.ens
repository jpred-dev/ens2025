
;Pruebas de validez del primer operando
XOR #0, .R0
XOR .R1, .R0
XOR /0, .R0
XOR [.R1], .R0
XOR #1[.IX], .R0
XOR #1[.IY], .R0

;Pruebas de validez del segundo operando
XOR #0, #0
XOR #0, .R0
XOR #0, /0
XOR #0, [.R0]
XOR #0, #1[.IX]
XOR #0, #1[.IY]
