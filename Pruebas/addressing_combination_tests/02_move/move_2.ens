;Pruebas de validez del primer operando
MOVE #0, .R0
MOVE .R1, .R0
MOVE /0, .R0
MOVE [.R1], .R0
MOVE #1[.IX], .R0
MOVE #1[.IY], .R0

;Pruebas de validez del segundo operando
MOVE #0, .R0
MOVE #0, /0
MOVE #0, [.R0]
MOVE #0, #1[.IX]
MOVE #0, #1[.IY]
